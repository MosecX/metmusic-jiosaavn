/// Native export for Hive. Uses hive_ce_flutter — an exact API-compatible fork
/// of hive_flutter, so it is the single maintained Hive line used on both
/// native and web builds (see hive_provider.dart).
library;
export 'package:hive_ce_flutter/hive_ce_flutter.dart';
